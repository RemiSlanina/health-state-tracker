[project]
name = "health-state-tracker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "psycopg>=3.3.4",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["."]